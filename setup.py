# setup.py (ملف جديد)
import sys
from database import init_db, seed_db

try:
    print("--- 🛠️ بدء التهيئة من ملف setup.py ---")
    init_db() # إنشاء الجداول وحذف القديم منها (لأننا أضفنا DROP TABLE)
    seed_db() # تعبئة البيانات الجديدة
    print("--- 🟢 تم إعداد قاعدة بيانات SQLite3 بنجاح ---")
except Exception as e:
    print(f"ERROR: فشل في تهيئة قاعدة البيانات: {e}")
    sys.exit(1)
